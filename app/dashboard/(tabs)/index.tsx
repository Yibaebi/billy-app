import ByText from '@/components/ui/Text';
import { View } from 'react-native';

export default function Dashboard() {
  return (
    <View className="flex-1 bg-secondary-100">
      <ByText>Dashboard</ByText>
    </View>
  );
}
