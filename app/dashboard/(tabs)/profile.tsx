import ByText from '@/components/ui/Text';
import { View } from 'react-native';

export default function Settings() {
  return (
    <View className="flex-1 bg-red-500">
      <ByText>Settings</ByText>
    </View>
  );
}
